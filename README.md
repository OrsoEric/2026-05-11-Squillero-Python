O grande is strictly bigger doesn't say much

Theta says it's bound within two constants of the big o

K_U * Theta(x) > f(x) > K_L * Theta(x)


# heapify

it's a binary tree with sorted fruits
good for priority queues


# VENV

uv venv .venv --python 3.13

.venv\Scripts\activate

# excercise

patient come

patient have a priority

with same priority arrival order

use dataclass

